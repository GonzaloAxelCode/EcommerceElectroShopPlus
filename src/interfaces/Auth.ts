export interface App {
  title: string;
}
export interface StateTypesAuth {
  access: any;
  refresh: any;
  isAuthenticated: Boolean | any;
  user: any;
  loading: boolean;
}

export interface User {}
